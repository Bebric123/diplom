version: '3'
services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn backend.src.api.main:app --host 0.0.0.0 --port 8000