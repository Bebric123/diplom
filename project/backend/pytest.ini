[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: тесты с реальной PostgreSQL (нужен TEST_DATABASE_URL)
    load: нагрузочные сценарии (Locust), не pytest
